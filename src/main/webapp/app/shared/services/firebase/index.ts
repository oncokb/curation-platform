import { FirebaseContext, FirebaseProvider, useFirebase } from './firebase.context';
import Firebase from './firebase';

export default Firebase;

export { FirebaseContext, FirebaseProvider, useFirebase };
